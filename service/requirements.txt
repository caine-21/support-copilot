# Optional deps for the ticket workflow API slice (service/).
# The core agent stack already lives in the repo requirements.txt.
# Run:  py -m pip install -r service/requirements.txt
fastapi>=0.115
uvicorn>=0.32
httpx>=0.25
